interface:
  display_name: "Conversation History"
  short_description: "Summarize agent sessions for handoff or PRs"
  default_prompt: "Use $conversation-history to summarize the key prompts, decisions, and pivots from this session."
